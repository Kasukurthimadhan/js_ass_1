function problem9(input){
    if(input[0]=='R' || input[0]=='r'){
        console.log(`${input} Plays banjo`);
    }
    else{
        console.log(`${input} does not play banjo`);
    }
}

problem9("avi")